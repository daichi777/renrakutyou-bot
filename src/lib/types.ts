export interface Child {
  id: string;
  name: string;
  name_reading: string;
  birth_date: string;
  birth_order: string;
  personality: string | null;
  notes: string | null;
  created_at: string;
}
